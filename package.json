{
  "name": "ortho-rounds-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted LAN server with SQLite storage for the Ortho Rounds patient tracker. Zero npm dependencies.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --no-warnings server.js",
    "dev": "node --no-warnings --watch server.js"
  }
}
