{
  "name": "ortho-rounds-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted server for the Ortho Rounds patient tracker. SQLite by default; optional MongoDB backend for persistent cloud hosting.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --no-warnings server.js",
    "dev": "node --no-warnings --watch server.js",
    "test": "node --no-warnings --test"
  },
  "dependencies": {
    "mongodb": "^7.5.0",
    "web-push": "^3.6.7"
  }
}
